import { useCallback, useState } from 'react'; import { useFocusEffect } from '@react-navigation/native'; import { Card, Text } from 'react-native-paper'; import { Screen } from '../components/Screen'; import { ErrorNotice } from '../components/ErrorNotice'; import { apiError, mobileApi } from '../services/api'; import type { Order } from '../types';
export function OrdersScreen() { const [orders, setOrders] = useState<Order[]>([]); const [error, setError] = useState<string>(); useFocusEffect(useCallback(() => { mobileApi.orders().then(setOrders).catch(e => setError(apiError(e).message)); }, [])); return <Screen><Text variant="headlineSmall">My orders</Text><ErrorNotice message={error} />{orders.map(o => <Card key={o.id}><Card.Content><Text variant="titleMedium">{o.title} × {o.quantity}</Text><Text>{o.delivery_status} · ₱{Number(o.price).toFixed(2)}</Text></Card.Content></Card>)}</Screen>; }
